/** 앱 전역 색·타이포 (스토리보드의 골드 포인트 등은 여기서 조정) */
export const colors = {
  background: '#fff',
  text: '#111',
  accent: '#c9a227',
  border: '#e8e8e8',
} as const;

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
} as const;
