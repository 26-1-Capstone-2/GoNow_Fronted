# 2026-08-21: 과거 이 파일에 카카오 REST API 키(EXPO_PUBLIC_KAKAO_REST_API_KEY)가 실제 값으로
# 커밋돼 있던 걸 발견해 키 재발급 + .env.local로 이전함(BUGS.md 버그50).
# 2026-08-23: 프론트가 카카오를 직접 호출하지 않고 스프링 프록시(/api/places/search/*)를 거치도록
# 바뀌면서 이 키 자체가 프론트에서 완전히 필요 없어짐 — .env.local에서도 제거됨.